STEP 1: Install required libraries using the following command.
    pip install -r requirements.txt
    
STEP 2: Run the simulator using one of the following commands
    python  RunMe.py
    python3 RunMe.py
    
    
